<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :technology %><br>
    <%= f.text_field :technology %>
  </div>
 <div class="field">
    <%= f.label :profiency %><br>
    <%= f.select(:profiency, ['Academic', 'Professional']) %>
  </div>
  <div class="field">
    <%= f.label :scale %><br>
    <%= f.text_field :scale %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
